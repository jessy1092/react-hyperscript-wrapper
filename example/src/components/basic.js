
import {wrapper} from 'react-hyperscript-wrapper';

const basic = {
  div: 'div',
  button: 'button',
  input: 'input',
  p: 'p',
  li: 'li',
  ul: 'ul',
  a: 'a',
  span: 'span'
}

const basicComponent = wrapper(basic);

export default basicComponent;
